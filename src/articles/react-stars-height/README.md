## 第一轮

请根据以下需求生成一个完整的 Remotion 项目，用于展示 React GitHub Star 从 0 增长到 243,550 的过程，通过一根垂直柱子的高度来比喻 star 数量（1 star = 1 米），并在柱子旁标注关键高度参照物。

### 1. 视频基本信息

- **标题**：React GitHub Star ⭐ 增长趋势 2013 至 2026
- **核心动画**：柱子的高度从 0 线性增长到代表 243,550 star 的高度（即 243,550 米）。柱子的颜色使用 React 主题色 `rgb(8, 126, 164)`。

### 2. 画面布局（三块区域）

#### 顶部区域

- 显示标题文字：“React GitHub Star ⭐ 增长趋势 2013 至 2026”

#### 中间区域（柱子）

- 绘制一根垂直的柱子，**宽度 80px**，底部固定在地面线（y = 底部），高度根据当前帧对应的 star 数动态变化。
- 柱子颜色：`rgb(8, 126, 164)`，可添加轻微渐变增加质感。
- 在柱子的**顶部下方约 20px 处**放置 React 标志（可用文字“React”代替，字体稍小，白色）。
- 在柱子的**顶部上方约 30px 处**显示当前帧对应的 star 数量，格式为带千位分隔符的数字，后面加一个星号（例如 `243,550 ★`）。该数字应随柱子高度实时更新。

#### 左边区域（参照物）

- 参照物为建筑物统一用相同 svg 画出来，不同建筑使用相同即可，顶部标注名称和高度区分。
- 参照物为山用 svg 画出来，不同的山使用相同 svg 即可，顶部标注名称和高度区分。
- 参照物为民航客机用 svg 画出来。
- 平流层和中间层用虚线+文字画出来。
- 参照物为卫星用 svg 画出来。

- 标注参照物的名称和高度（单位自动换算：超过 1000 米时显示为 `x.x km`，否则显示为 `x m`）。
- 参照物列表（按高度升序）：
  1. `7层住宅楼` — 高度 22 米
  2. `上海国际商贸中心T2` — 高度 370 米
  3. `上海中心大厦` — 高度 632 米
  4. `哈利法塔` — 高度 828 米（用醒目的颜色，如 `#FFAA00`）
  5. `泰山` — 高度 1545 米
  6. `富士山` — 高度 3776 米
  7. `玉龙雪山` — 高度 5596 米
  8. `念青唐古拉山` — 高度 7162 米
  9. `乔戈里峰` — 高度 8611 米
  10. `珠穆朗玛峰` — 高度 8,848 米
  11. `民航客机` — 高度 10000 米
  12. `平流层顶` — 高度 50,000 米（50 km）
  13. `中间层顶` — 高度 85,000 米（85 km）
  14. `卡门线（太空边界）` — 高度 100,000 米（100 km）
  15. `低地球轨道卫星` — 高度 200,000 米（200 km）

### 3. 代码结构与输出

- 代码生成在 `src/articles/react-stars-height/ReactStarsComposition.tsx` 文件中。
- React star 数据在 `src/articles/react-stars-height/data/starData.ts` 文件的 `reactStarsMonthly` 中。
- 保证一个组件不超过 200 行代码，超过时拆分成多个组件。

### 4. 画面要求

- 初始时地面线可画一条粗线，并标注“地面 0 m”，左侧显示 22米 `7层住宅楼`。
- 画面中间 React star 柱子（以后简称“柱子”）初始
- 镜头从下往上移动，React star 柱子一开始只能看到 React emoji（`⚛`）+不断增长的 star 数量，顶部端点看不到。
- 随着 star 数增长，柱子高度也线性增长，直到达到代表 243,550 star 的高度。
- 柱子左侧的参照物，随着 star 数增长，逐个按照顺序出现和消失
- 当 star 数完结，视频结束，柱子顶部端点才出现在画面中。
- 背景使用从深蓝色到黑色的径向渐变，模拟从地面到太空的感觉。

### 5. 额外要求

---

请根据以上 Prompt 生成完整的 Remotion 项目代码。

## 第二轮

现在存在几个问题：

- 地面和参照物也跟着上升了，正确应该是：地面永远在视频的底部，不随柱子高度变化。

其次动画逻辑修改成：

- 初始化展示主参照物即第1个参照物：22 米的 `7层住宅楼`，高度是视频高度的一半。
- 当 react star 柱子高度增长到 22 米时，第 1 个建筑物右侧出现第2个参照物即 370 米的 `上海国际商贸中心T2`，主参照物变成第2个，与此同时第1个参照物和 react star 柱子高度对比第2个参照物的高度比例缩小即高度变矮，即此时第1个参照物和 react star 柱子的高度分别是他们之前的高度的 `22/370`。以此类推。
- 所有参照物高度不能超过视频高度的一半
- 主参照物高度永远是视频高度的一半，其他参照物高度根据主参照物高度比例缩放。
- 参照物数据在 src/articles/react-stars-height/data/landmark.ts 的 landmarks 数组中
