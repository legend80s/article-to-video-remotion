

请根据以下需求生成一个完整的 Remotion 项目，用于展示 React GitHub Star 从 0 增长到 243,550 的过程，通过一根垂直柱子的高度来比喻 star 数量（1 star = 1 米），并在柱子旁标注关键高度参照物。

### 1. 视频基本信息
- **标题**：React GitHub Star ⭐ 增长趋势 2013 至 2026
- **核心动画**：柱子的高度从 0 线性增长到代表 243,550 star 的高度（即 243,550 米）。柱子的颜色使用 React 主题色 `rgb(8, 126, 164)`。

### 2. 画面布局（三块区域）

#### 顶部区域
- 显示标题文字：“React GitHub Star ⭐ 增长趋势 2013 至 2026”
- 字体：粗体，白色，适当大小，居中。

#### 左边区域（柱子）
- 绘制一根垂直的柱子，**宽度 80px**，底部固定在地面线（y = 底部），高度根据当前帧对应的 star 数动态变化。
- 柱子颜色：`rgb(8, 126, 164)`，可添加轻微渐变增加质感。
- 在柱子的**顶部下方约 20px 处**放置 React 标志（可用文字“React”代替，字体稍小，白色）。
- 在柱子的**顶部上方约 30px 处**显示当前帧对应的 star 数量，格式为带千位分隔符的数字，后面加一个星号（例如 `243,550 ★`）。该数字应随柱子高度实时更新。

#### 右边区域（参照物）
- 在画布右侧（柱子右侧）绘制一系列**水平虚线**，每条线代表一个关键高度参照物。
- 每条虚线左侧与柱子相交（或从柱子边缘向右延伸），右侧标注参照物的名称和高度（单位自动换算：超过 1000 米时显示为 `x.x km`，否则显示为 `x m`）。
- 参照物列表（按高度升序）：
  1. `7层住宅楼` — 高度 22 米
  2. `上海中心` — 高度 632 米
  3. `默迪卡118` — 高度 678.9 米
  4. `哈利法塔` — 高度 828 米（用醒目的颜色，如 `#FFAA00`）
  5. `珠穆朗玛峰` — 高度 8,848 米
  6. `平流层顶` — 高度 50,000 米（50 km）
  7. `中间层顶` — 高度 85,000 米（85 km）
  8. `卡门线（太空边界）` — 高度 100,000 米（100 km）
  9. `低地球轨道卫星` — 高度 200,000 米（200 km）
- 每条虚线的颜色建议使用半透明白色或浅灰色，文字标注颜色与虚线一致或使用白色。

### 3. 坐标与比例计算
- 假设柱子底部为 `y = 950`（1080p 底部预留 130px 给地面装饰），柱子顶部最高（对应最终高度 243,550 米）为 `y = 100`。
- 因此画布可用高度为 `950 - 100 = 850px`，对应实际高度 `243,550 米`。
- 比例尺：`1px = 243550 / 850 ≈ 286.53 米`。
- 每个参照物的画布 y 坐标计算公式：`y = 950 - (height / 286.53)`。
  - 注意：当高度为 0 时 y = 950（地面），高度为 243,550 时 y = 100（顶部）。
- 柱子高度（像素）= `(currentStar / 286.53)`，但要注意柱子底部固定，所以柱子的矩形高度即为 `950 - y` 的差值。

### 4. 动画逻辑
- 使用 `interpolate()` 函数根据当前帧索引计算当前的 star 数量：从 `0` 到 `243550`，线性增长。
- 当前帧索引范围：0 ～ 149（共150帧）。
- 为了增强动感，可以在前 30 帧使用 `spring` 实现一个轻微的“弹跳”起始效果（可选）。

### 5. 代码结构与输出
请生成以下文件（Remotion 项目标准结构）：
- `package.json`：包含必要的依赖（remotion, react, typescript 等）。
- `remotion.config.ts`：基础配置。
- `src/index.tsx`：注册视频组件。
- `src/Video.tsx`：主视频组件，包含所有画面元素和动画逻辑。
- `src/constants.ts`：定义参照物数组、主题色、最大星数等常量。
- 所有代码需包含详细的注释，便于理解和修改。

### 6. 额外要求
- 确保数字格式化为千位分隔符（例如 `243,550`）。
- 文字清晰可读，避免重叠。对于底部密集的参照物（如 22m、632m 等），允许它们的标注稍微偏移以避免完全覆盖。
- 地面线可画一条粗线，并标注“地面 0 m”。
- 背景使用从深蓝色到黑色的径向渐变，模拟从地面到太空的感觉。

---

请根据以上 Prompt 生成完整的 Remotion 项目代码。
